architectury {
	common('fabric', 'neoforge')
}

dependencies {
	// compile-time only: provides the Mixin library. Loader classes are never
	// called from common code (Platform interface + ServiceLoader instead).
	modImplementation "net.fabricmc:fabric-loader:${rootProject.loader_version}"

	// YACL API for the config screen; optional at runtime on both loaders
	modCompileOnly "dev.isxander:yet-another-config-lib:${rootProject.yacl_version}-fabric"
}
